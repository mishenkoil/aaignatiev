/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  reactStrictMode: true,
  basePath: '/aaignatiev',
  assetPrefix: '/aaignatiev/',
  trailingSlash: true,
};

export default nextConfig;
